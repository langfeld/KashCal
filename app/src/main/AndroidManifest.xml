<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Package visibility: Allow querying for geo: handlers (API 30+) -->
    <!-- Required for openInMaps() to detect installed map applications -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <!-- Network: CalDAV sync -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Reaching a CalDAV server on the local network (self-hosted). Android 17+
         blocks local-network sockets unless this runtime permission is granted;
         older versions implicitly grant it to apps holding INTERNET. Requested
         at sign-in only when the entered server URL is a local address. -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!-- Background sync with WorkManager -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Notifications for sync progress (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for reminder notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Exact alarm permission for calendar reminders (Android 12+) -->
    <!-- USE_EXACT_ALARM is auto-granted for calendar/alarm apps per Google Play policy -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Contact birthdays: Read contacts to generate birthday calendar -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- CardDAV contact sync: write synced contacts into a dedicated Contacts source -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- Device calendars: Read/write events from other calendar apps -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Sync adapter registration (for CalendarProvider intent routing) -->
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />

    <!-- Optional app lock: authenticate with biometric or device credential -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        Declare touchscreen as optional for Chromebook/tablet compatibility.
        Without this, app is filtered out of Play Store for non-touch devices.
    -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".KashCalApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KashCal">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.KashCal"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:windowSoftInputMode="adjustResize">

            <!-- The launcher + APP_CALENDAR entry lives on the activity-aliases below so the
                 app icon can be swapped by enabling exactly one alias at a time. MainActivity
                 keeps the file/text-share filters. -->

            <!-- Handle .ics file imports (MIME type) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/calendar" />
            </intent-filter>

            <!-- Plain-text shares from other apps. Routes through SharedTextNormalizer
                 into Quick Add (short input) or the full event form (long input). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Calendar-file shares from other apps (share sheet). Routes the
                 shared .ics from EXTRA_STREAM into the existing import sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/calendar" />
                <data android:mimeType="application/ics" />
                <data android:mimeType="text/x-vcalendar" />
            </intent-filter>

            <!-- Handle .ics file imports (content:// URI) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="text/calendar" />
            </intent-filter>

            <!-- Handle .ics file imports (file:// URI by extension) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.ics" />
            </intent-filter>

            <!-- Handle .ics (application/ics - email client attachments) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/ics" />
            </intent-filter>

            <!-- Handle .ics (text/x-vcalendar - legacy vCalendar format) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/x-vcalendar" />
            </intent-filter>

            <!-- Handle webcal:// subscription links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="webcal" />
            </intent-filter>

            <!-- Handle webcals:// (secure) subscription links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="webcals" />
            </intent-filter>

            <!-- Handle calendar event creation/editing from other apps -->
            <!-- Allows "Add to Calendar" from email clients, browsers, transit apps, etc. -->
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <action android:name="android.intent.action.INSERT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.dir/event" />
            </intent-filter>

            <!-- Handle EDIT/INSERT with item MIME type (some apps use item instead of dir) -->
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <action android:name="android.intent.action.INSERT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.item/event" />
            </intent-filter>

            <!-- Handle VIEW for specific CalendarProvider events (transit apps, notification taps) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.item/event" />
            </intent-filter>

            <!-- Handle time/epoch content URIs from CalendarContract (clock widget "Go to calendar") -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="time/epoch" />
                <data android:host="com.android.calendar" />
                <data android:scheme="content" />
            </intent-filter>

            <!-- Handle ACTION_INSERT with CalendarContract data URI -->
            <!-- Apps using setData(Events.CONTENT_URI) instead of setType() -->
            <intent-filter>
                <action android:name="android.intent.action.INSERT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:host="com.android.calendar" />
            </intent-filter>

            <!-- Handle CalendarContract VIEW intents (launchers, clock widgets) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:host="com.android.calendar" />
            </intent-filter>

            <!-- Handle CalendarContract EDIT intents (launcher "create event" button) -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:host="com.android.calendar" />
            </intent-filter>

            <!-- App Shortcuts (long-press launcher icon) -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>

        </activity>

        <!-- App-icon aliases. Exactly one is enabled at a time; switching the icon enables the
             chosen alias and disables the others (see AppIconUtility). The default alias is the
             only one enabled at install; the supporter variants share one gold icon and differ
             only in the launcher label. Each alias carries the LAUNCHER/APP_CALENDAR filter and
             the dynamic-shortcuts meta-data so shortcuts survive an icon swap. -->
        <activity-alias
            android:name=".MainActivityDefault"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_CALENDAR" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity-alias>

        <activity-alias
            android:name=".MainActivitySupporter"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_supporter"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_CALENDAR" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity-alias>

        <activity-alias
            android:name=".MainActivitySupporterCalendar"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_supporter"
            android:label="@string/app_icon_label_calendar"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_CALENDAR" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity-alias>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.KashCal"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!--
            Disable default WorkManager initialization.
            We provide custom configuration via KashCalApplication.workManagerConfiguration
            to use HiltWorkerFactory for dependency injection in workers.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            WorkManager foreground service with dataSync type.
            Required for foreground sync operations on Android 14+.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Reminder alarm receiver: handles alarm triggers -->
        <receiver
            android:name=".reminder.receiver.ReminderAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.onekash.kashcal.REMINDER_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Reminder action receiver: handles snooze/dismiss from notification -->
        <receiver
            android:name=".reminder.receiver.ReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.onekash.kashcal.SNOOZE_REMINDER" />
                <action android:name="org.onekash.kashcal.DISMISS_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Device calendar reminder alarm receiver -->
        <receiver
            android:name=".reminder.device.DeviceCalendarAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.onekash.kashcal.DEVICE_REMINDER_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Device calendar reminder action receiver: handles snooze/dismiss -->
        <receiver
            android:name=".reminder.device.DeviceCalendarReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.onekash.kashcal.DEVICE_SNOOZE_REMINDER" />
                <action android:name="org.onekash.kashcal.DEVICE_DISMISS_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver: reschedule reminders after device boot or app update -->
        <receiver
            android:name=".reminder.receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Timezone change receiver: update widget when timezone or time changes -->
        <receiver
            android:name=".widget.TimezoneChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>

        <!-- Midnight widget rollover: triggered by explicit PendingIntent from AlarmManager
             (setExactAndAllowWhileIdle), so no intent-filter needed. Uses AlarmManager
             instead of WorkManager so day-rollover fires through Doze. -->
        <receiver
            android:name=".widget.MidnightWidgetUpdateReceiver"
            android:exported="false" />

        <!-- Today's Agenda widget receiver -->
        <receiver
            android:name=".widget.AgendaWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/agenda_widget_info" />
        </receiver>

        <!-- Week View widget receiver -->
        <receiver
            android:name=".widget.WeekWidgetReceiver"
            android:exported="true"
            android:label="@string/week_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/week_widget_info" />
        </receiver>

        <!-- Month View widget receiver -->
        <receiver
            android:name=".widget.MonthWidgetReceiver"
            android:exported="true"
            android:label="@string/month_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/month_widget_info" />
        </receiver>

        <!-- Date widget receiver (icon-like, shows today's date) -->
        <receiver
            android:name=".widget.DateWidgetReceiver"
            android:exported="true"
            android:label="@string/date_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/date_widget_info" />
        </receiver>

        <!-- Upcoming Events widget receiver (10-day rolling horizon) -->
        <receiver
            android:name=".widget.UpcomingWidgetReceiver"
            android:exported="true"
            android:label="@string/upcoming_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/upcoming_widget_info" />
        </receiver>

        <!-- Account authenticator for CalendarProvider registration.
             Registers org.onekash.kashcal account type with Android.
             exported="false" — only the system account framework binds to this. -->
        <service
            android:name=".sync.adapter.KashCalAuthenticatorService"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/kashcal_authenticator" />
        </service>

        <!-- Sync adapter stub linking account type to com.android.calendar.
             Required for CalendarProvider URI intent routing on many devices.
             Auto-sync disabled — real sync is via WorkManager. -->
        <service
            android:name=".sync.adapter.KashCalSyncAdapterService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/kashcal_syncadapter" />
        </service>

        <!-- Account authenticator for the dedicated contacts source.
             Registers org.onekash.kashcal.contacts account type so per-login,
             email-named contacts accounts stay independent of the singleton
             calendar account. exported="false" — only the system account
             framework binds to this. -->
        <service
            android:name=".sync.adapter.KashCalContactsAuthenticatorService"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/kashcal_contacts_authenticator" />
        </service>

        <!-- Sync adapter stub linking the contacts account type to
             com.android.contacts. Required so Android ties the per-login
             contacts account to the Contacts Provider (and does not purge its
             RawContacts). Auto-sync disabled — real sync is via WorkManager. -->
        <service
            android:name=".sync.adapter.KashCalContactsSyncAdapterService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/kashcal_contacts_syncadapter" />
            <!-- Declares the account type as a recognized contacts source (its
                 edit-schema / displayable data kinds). Without this the account
                 registers but its contacts never surface under "Contacts to
                 display". -->
            <meta-data
                android:name="android.provider.CONTACTS_STRUCTURE"
                android:resource="@xml/contacts" />
        </service>

        <!--
            Quick Settings tile: fast event capture from the notification shade.
            Stateless launch tile (no toggle/ACTIVE_TILE) — onClick opens Quick Add.
        -->
        <service
            android:name=".widget.QuickAddTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_tile_calendar"
            android:label="@string/qs_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!--
            FileProvider for secure file sharing (ICS exports).
            Required for sharing files via Intent on Android 7.0+.
            Uses content:// URIs instead of file:// URIs.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
